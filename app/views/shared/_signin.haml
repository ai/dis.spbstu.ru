%aside#controls.signin
  .wrap
    .hole
      .rotator
        .light
        .shadow
        .signin-form
          - if flash[:wrong_user]
          - elsif flash[:wrong_auth]
          - else
            Редакция
          = render 'shared/signin_methods'
